# Install a mutually compatible torch/torchvision pair for the target CPU/CUDA
# platform before this file. Keeping them out of the generic resolver avoids
# silently replacing a deliberately selected CUDA wheel.
numpy>=1.23,<2
scipy>=1.9
pandas>=1.5,<3
scikit-learn>=1.1
Pillow>=9.0
opencv-python-headless>=4.6,<4.12
PyYAML>=6.0
tqdm>=4.64
GitPython>=3.1

# Lossless compressed SDD caches additionally require the ``zstd`` command
# (for example: ``conda install -c conda-forge zstd``).

# Legacy pixel-space augmentation (used only by the independent baseline).
albumentations>=1.3,<2

# Logging, scientific visualization and verification.
wandb>=0.13
matplotlib>=3.6
pytest>=7.0
